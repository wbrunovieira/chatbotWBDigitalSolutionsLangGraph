# Test-only dependencies. Not installed into the production image
# (the Dockerfile installs requirements.txt only).
-r requirements.txt

pytest==9.0.2
pytest-asyncio==1.3.0
fakeredis==2.35.0

# MCP server (mcp_server.py exposes the agent tools over the Model Context Protocol)
mcp==1.28.1
